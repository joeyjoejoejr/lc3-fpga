# Applications

Planned frontend applications:

- `desktop`: native simulator/debugger UI
- `web`: browser simulator/debugger UI

Both should reuse the assembler, image-format code, and simulator API crates.
